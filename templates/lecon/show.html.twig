{% extends 'base.html.twig' %}

{% block title %}Lecon{% endblock %}

{% block body %}
    <h1>Leçon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lecon.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ lecon.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ lecon.description }}</td>
            </tr>
        </tbody>
    </table>

    <div>
        <a class="btn btn-outline-primary" href="{{ path('app_lecon_index') }}">Retour</a>

        <a class="btn btn-outline-success" href="{{ path('app_lecon_edit', {'id': lecon.id}) }}">Modifier</a>

    </div>
{% endblock %}
